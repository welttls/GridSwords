# 《炼剑》SwordForge · 版本更新说明

> 工作流约定：**每次 git 提交都必须在此文件顶部新增一条版本说明**，并在 commit message 中附上版本号。
> 版本号规则：功能新增 → 次版本号 +1（x.y.0）；仅修复/微调 → 修订号 +1（x.y.z）。

---

## v1.6.1 (2026-08-10)

### 🐛 修复（代码审查专项，含 P0 严重问题）

- **僵尸剑意**：击杀后防御者未立即移除，残留可再行动一轮、虚高种群计数。现击杀即 `removeSword`
- **剑诀体系接通**：`counter`(后手反击)/`thunder`(雷引)此前完全无效果；`strike`(前50回合攻+20%)/`agile`(攻-10%能耗-50%)/`quick`(攻+10%能耗-20%) 按描述修正
- **新局覆盖存档**：有进行中的局时点「开始炼剑」会先弹确认，不再静默覆盖
- **开局存档时序**：`activeRun` 首存不再为 false（避免开新局 5 秒内关页丢局）
- **万剑榜 history 无限增长**：截断结果重新赋值
- **HUD 日志监听泄漏**：`destroy()` 解绑，多局不再重复触发日志/稀有词条 toast
- **剑名 HTML 注入**：大比结果改 `textContent` 渲染，防自 XSS
- **续档生态重置**：存档含边界/庚金/火墙/天劫开关，续玩不再「剑域回春」
- **续档行为统计丢失**：行为统计随剑意序列化，鉴定标签/评分续档不失真
- **存档字段迁移**：旧档缺 `origin` 自动按根剑补 `seed`，不再误判「本命血脉已绝」
- **自动存档计时**：按真实帧时长累计（原固定 16.6ms 随帧率漂移）
- **投食空投不扣配额**：按实际落下的团数计
- **「罢了」死局入口**：放弃后清 `activeRun`，不再残留「继续炼剑」死循环
- **大比得分 `hpRatio`**：按实际剑体上限而非硬编码 100
- **Duel 终结事件顺序**：先出招文本、后判胜负，不再顺序颠倒
- **悟道之树断裂**：种子剑胚补 `lineage` 条目；空位不足时重种有明确反馈

### ⚖️ 平衡调整
- **无根水身法生效**：不再只增精元消耗，改为每 tick 有几率额外行动一步（移动更迅疾），与文案「身法 +0.5」相符
- **淬毒门槛**：锋锐 ≥ 8 且杀性 ≥ 0.65 且久历杀伐（原 7/0.6 凶潮前两日多剑悟毒）

### 🧹 清理
- 死代码：`spawnFoodAt`/`spawnFoodAround`、`minimized` 死类、`npcHp` 死变量
- `swordIcon` 按 canvas 代际防异步重绘竞态；`techTitle` 定时器清理
- 炉材面板缺键口径统一、剑潮投放全失败时给日志、场景切换销毁渲染器
- 新增 `AI_HANDOFF.md`（AI 接力工作手册）

---

## v1.6.0 (2026-08-10)

### ✨ 属性语义重构（锋锐=攻击 / 坚韧=防御 / 感知=闪避）
- **属性各归其位**：锋锐=攻击力（含暴击）、坚韧=防御力、感知=闪避率+视野、速度=蓄条/移动、杀性=凶性（暴击）
- **大比闪避改感知差**：`clamp((守方感知−攻方感知)×0.06+0.08, 5%, 45%)`，游斗剑诀额外 +12% 后仍钳制上限；移除原速度闪避
- **大比暴击改锋锐+杀性**：`clamp(招式加成+锋锐×0.02+杀性×0.15, 0, 40%)`，感知不再加成暴击
- **野外新增感知闪避**：`clamp((守方感知−攻方感知)×0.04+0.05, 0, 35%)`，成功→伤害0+攻方扑空反震+守方得后手反击之势；感知范围（视野）保留
- **大比对手剑镜像翻转**：右侧对手剑 `scaleX(-1)`，两剑相对而立
- 灵鉴属性 tooltip 文案同步更新（闪避/暴击语义）
- **平衡重测**：无干预 avg6 / 扶持 avg15（扶持下 2/4 局「强」），感知闪避未破坏生态

---

## v1.5.0 (2026-08-10)

### ✨ 新增
- **宗门大比 · 水墨战斗背景**：接入用户提供的水墨山水背景图（`pic/background1.png`、`pic/background2.jpg` → `public/img/battle/`），每场决斗随机选用一张，覆盖决斗场地（`.duel-bg` 层 + 底部暗化渐变，剑与特效浮于其上）

### 🐛 修复
- **回复/护盾不应造成伤害**：此前回春术/吞金燃灵/百炼守/磐石护等辅助招式也会对对方造成少量伤害（如「回春术……造成 7 点剑伤」）。现辅助招式（`recover`/`guard` 类）完全不出击——只回复自身/架设护盾，伤害为 0，MUD 文字改为「青崖施展「回春术」，剑体回复 49 点。」；护盾照常生效（下次受击大幅减免）

---

## v1.4.0 (2026-08-10)

### ✨ 新增
- **宗门大比 · 技能专属特效**：每个招式不再只是「剑动一下」，按技能类型播放独立动画——
  - 剑气斩 → 金色弧光弹道横贯场地
  - 天门破 → 贯穿光束
  - 焚天爆 → 爆炸扩散火圈
  - 寄灵噬 → 紫色吸血线 + 攻方回复闪光
  - 淬毒雨 → 绿色毒雾弥漫
  - 回春术/吞金燃灵 → 回复光柱
  - 磐石护/百炼守 → 金色护盾脉冲
  - 瞬水步/游龙步 → 残影连闪
  - 重击（破军斩等）→ 冲击波扩散
  - 回复/护盾类招式不再前冲出击，只在自己身上释放特效
- **CHANGELOG.md 版本更新说明**：确立「每次提交更新版本说明」工作流

### 🐛 修复
- **百炼之体与「历经」战斗数不一致**：此前「历经」只统计主动攻击（attackCount），而百炼之体判定用被动挨打次数（fightsSurvived），导致显示 11 战却已悟得「历经≥15战」词条。现统一为 **总经历战斗数 = 主动攻击 + 防御应战**，灵鉴显示与词条门槛完全一致
- **本命血脉断绝弹窗点「罢了」**：原仅关闭弹窗继续挂机，现返回主菜单

---

## v1.3.0 (2026-08-10)

### ✨ 新增
- **剑意技能融入宗门大比**：`Duel.buildTechniques` 接入技能系统——五行天赋技能（剑气斩/回春术/瞬水步/焚天爆/磐石护）成为大比必得招式，词条技能（天门破/百炼守/游龙步/吞金燃灵/淬毒雨/寄灵噬）替代原词条招，招式组成 = 锋行 + 五行天赋 + 词条(优先) + 属性(补位)，最多 4 招
- **灵鉴「剑技」区块**：展示该剑全部技能（名称/描述/来源/耗精元/冷却时辰），与野外实际施放的剑技同源

---

## v1.2.0 (2026-08-10)

### ✨ 新增
- **剑意阶段技能系统**（`src/simulation/Skills.ts`）：
  - 五行天赋技能 5 个（剑气斩/回春术/瞬水步/焚天爆/磐石护）
  - 词条衍生技能 6 个（天门破/百炼守/游龙步/吞金燃灵/淬毒雨/寄灵噬）
  - 耗精元施放、带冷却、触发条件智能（buff 需敌临近防刷屏、AoE 需敌入半径防空放）
  - 野外粒子特效（弹道/环形/光束/瞬移闪光/buff 光环）

---

## v1.1.0 (2026-08-10)

### ✨ 新增
- **响应式布局**：16:9 比例修复，画布等比缩放，≤900px 竖排可滚动
- **粒子效果系统**：碰撞/分化/陨落/吃食/天劫粒子（Pixi）
- **宗门大比仪式感重做**：左右双剑 + ATB 半即时选招 + 文字 MUD + 前冲/受击/大招大字
- **剑形素材**：game-icons 五行剑 SVG
- **体验优化**：剑域纪事重要 tab、日志定向聚焦、词条在剑谱展示、修仙计时存续

---

## v1.0.0 (2026-08-10)

### ✨ 首版
- **完整单局闭环**：择剑胚 → 十日炼剑（涌现模拟/词条参悟/精元经济/天劫收束）→ 剑成鉴定 → 宗门大比 → 万剑榜
- 涌现式玩法：十日可能一无所获，一旦出现能自复制+自采气的稳定剑意即惊喜
